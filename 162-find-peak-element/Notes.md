<h2>find-peak-element Notes</h2><hr>Use Binary Search     
<br/>
Even if an array contains multiple peak elements, this algorithm will return only one peak index. It does not guarantee returning the first peak, the last peak, or the highest peak. Any valid peak element can be returned. The binary search process narrows the search space until it reaches one peak, and that peak's index is returned.
